index.html
<!DOCTYPE html>
<html>
<head>
  <title>SafeRide Dashboard</title>
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <div class="container">
    <h1>SafeRide</h1>

    <!-- Step 1 -->
    <div id="uploadScreen" class="screen active">
      <h2>Upload Ride</h2>
      <input type="file" id="rideFile">
      <button id="uploadBtn">Upload & Get Score</button>
    </div>

    <!-- Step 2 -->
    <div id="scoreScreen" class="screen">
      <h2>Your Score</h2>
      <p id="scoreText">Score: --</p>
      <p id="rewardText">Reward: --</p>
      <button id="claimBtn">Claim Reward</button>
    </div>

    <!-- Step 3 -->
    <div id="doneScreen" class="screen">
      <h2>✅ Reward Claimed</h2>
      <p id="txText"></p>
      <button id="homeBtn">Upload Another Ride</button>
    </div>
  </div>

  <script src="config.js"></script>
  <script src="script.js"></script>
</body>
</html>


styles.css
body {
  font-family: Arial, sans-serif;
  background-color: #f5f6fa;
  display: flex;
  justify-content: center;
  padding-top: 60px;
}
.container {
  background: white;
  padding: 20px;
  border-radius: 12px;
  box-shadow: 0 0 8px rgba(0,0,0,0.1);
  width: 300px;
  text-align: center;
}
button {
  background: #4f46e5;
  color: white;
  border: none;
  border-radius: 8px;
  padding: 10px;
  margin-top: 10px;
  cursor: pointer;
}
.screen { display: none; }
.active { display: block; }



config.js
// Your teammates will give you their backend link
window.BACKEND_URL = "https://your-teammate-backend.repl.co";



script.js
const screens = {
  upload: document.getElementById("uploadScreen"),
  score: document.getElementById("scoreScreen"),
  done: document.getElementById("doneScreen"),
};

function show(screen) {
  Object.values(screens).forEach(s => s.classList.remove("active"));
  screens[screen].classList.add("active");
}

document.getElementById("uploadBtn").onclick = async () => {
  // pretend we send to backend:
  const fakeResponse = { score: 85, reward: 10 };
  document.getElementById("scoreText").innerText = `Score: ${fakeResponse.score}`;
  document.getElementById("rewardText").innerText = `Reward: ${fakeResponse.reward} SRD`;
  show("score");
};

document.getElementById("claimBtn").onclick = async () => {
  // pretend claim worked
  document.getElementById("txText").innerText = "Transaction complete (mock)";
  show("done");
};

document.getElementById("homeBtn").onclick = () => show("upload");





